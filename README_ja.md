# 短歌画像ジェネレーター

ブラウザ上で短歌を縦書き画像として作成し、PNGで保存できるツールです。

## 概要
短歌画像ジェネレーターは、入力した短歌をHTML Canvasに描画し、画像として書き出します。ビルドやサーバーは不要で、`index.html` を開くだけで利用できます。

## 主な機能
- 日本語短歌向けの縦書き描画
- 入力と同時にプレビュー更新
- フォント選択（Google Fonts）
- 文字色・背景色の変更
- レイアウトプリセット:
  - 複数行向け
  - 一行向け
- 詳細モードでの文字設定入力
- 出力サイズ切り替え:
  - 800 x 800
  - 960 x 1280
- ワンクリックPNG保存（`my-tanka.png`）

## 使い方
1. このリポジトリを取得します。
2. ブラウザで `index.html` を開きます。
3. 短歌とデザイン設定を調整します。
4. 「画像を保存する」をクリックして保存します。

追加のインストールやビルド作業は不要です。

## 利用時のメモ
- 縦書きの見た目を安定させるため、句ごとに改行して入力するのがおすすめです。
- 入力内容や設定を変更すると、キャンバスが即時再描画されます。
- 現在の実装では、詳細設定値はプリセットと同期しつつ、描画は中央配置で行われます。

## ファイル構成
- `index.html` - 画面構成
- `style.css` - スタイル定義
- `script.js` - 描画ロジックとUI連携
- `README.md` - 英語版ドキュメント
- `README_ja.md` - 日本語版ドキュメント

## 技術スタック
- HTML5
- CSS3
- Vanilla JavaScript
- HTML Canvas API

## 対応ブラウザ
以下をサポートする主要ブラウザで動作します。
- Canvas API
- `document.fonts`

## 今後の改善案
- 保存ファイル名のカスタマイズ
- 詩形テンプレートの追加
- 縦書き時の句読点・小書き文字の位置調整強化

